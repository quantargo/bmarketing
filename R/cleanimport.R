# function which imports data set and conducting some data quality checks :
# * return an error if the target variable contains any missing values (NA’s).
# * warnings for all other variables which contain NA’s
# * columns which contain more than 50% NA’s will be removed

cleanimport <- function(){
  
  
}